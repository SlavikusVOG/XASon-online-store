import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class CookiesService {
  // public addCookie(name: string, value: string, expires: Date) {
  //   cookieStore.set(name, value, expires);
  // }
}
