# Handle line endings automatically for files detected as text 
# and force LF on checkout and commit.
* text=auto eol=lf

# Explicitly set binary files to prevent corruption
*.png binary
*.jpg binary
*.gif binary
